<tags_view>
<script>

export default{
    setTagWords( tagWords ){
        this.state.tagWords = tagWords
        this.update()
    }
}
</script>

<div class="sidebar">
    <p>Popular Tags</p>
    <div class="tag-list">
        <a each={ tag in state.tagWords } href="#/articles/tag/{ tag }" class="tag-pill tag-default">{ tag }</a>
    </div>
</div>

</tags_view>